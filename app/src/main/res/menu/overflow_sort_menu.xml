<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <!-- Settings, must always be in the overflow. -->
    <item android:id="@+id/action_title"
        android:title="@string/action_sort_title"
        app:showAsAction="never"/>

    <item android:id="@+id/action_popularity"
        android:title="@string/action_sort_popularity"
        app:showAsAction="never"/>

    <item android:id="@+id/action_runtime"
        android:title="@string/action_sort_runtime"
        app:showAsAction="never"/>

</menu>